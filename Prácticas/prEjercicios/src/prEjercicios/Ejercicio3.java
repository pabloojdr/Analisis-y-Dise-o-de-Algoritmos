package prEjercicios;

/* Dado un vector V de n elementos (no necesariamente ordenables), se dice que un
 * elemento x es mayoritario en V cuando el número de veces que x aparece en V es
 * mayor que n/2. Escribir un algoritmo que en tiempo O(n lg n) decida si un vector
 * tiene un elemento mayoritario y lo devuelva si lo tiene
*/

public class Ejercicio3 {
	 
	public int mayoritario(int[] v, int primero, int ultimo) {
		
	}
}
